#include <stdio.h>

/** main - Entry point of the program
 * This function serves as the entry point for the program. It prints
 * the lowercase alphabet, followed by the uppercase alphabet, and a new line.
 * It uses the 'putchar' function for character output.
 * Return: Always 0 (Success)
 */
int main(viod)
{       
	int n = 97;
	int m = 65;

while (n <= 122)
}
	putchar(n);
}
while (m <= 90)
{
	putchar(m);
}
	putchar('\n');
	return (0);
}

